# 테스트 프로파일: 하드웨어/외부 프로세스 없이 실행되도록 카메라/MediaMTX 비활성화.
# camera.enabled=false -> NoopStreamPublisher 주입 (ffmpeg/웹캠 불필요)
camera.enabled=false
mediamtx.autostart=false
# 테스트에서 DeviceProbe 가 실제 ffmpeg 를 실행하지 않도록(장치목록 비움) — 카메라 선택 검증 결정론적.
camera.ffmpeg=no-such-ffmpeg-for-tests

logging.level.com.servert=DEBUG
